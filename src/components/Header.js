import React from 'react';

const Header = () => (
  <header>
    <h1 className="header-title">todos</h1>
    <p className="p-header">Items will persist in the browser local storage</p>
  </header>
);

export default Header;
